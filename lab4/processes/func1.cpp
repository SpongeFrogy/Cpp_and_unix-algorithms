#include <windows.h>
#include <iostream> //standart
#include <cmath> //for pow()
#include <time.h> //magure time
#include <stdlib.h> // random number from _start to _end
 
using namespace std;
 
 
int main(int argc, char *argv[])
 
{
    double res1 = pow(x, 2) - pow(x, 2) + pow(x, 4) - pow(x, 5) + x + x;
    
 
}